---
import { getIconHtml } from "./icon-html";
interface Props {
  iconClass: string;
  value?: number | undefined;
}

const { iconClass, value } = Astro.props;
const htmlText = getIconHtml(iconClass, value);
---

<span set:html={htmlText} />
